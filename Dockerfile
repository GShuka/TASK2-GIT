# Используем базовый образ, включающий необходимые инструменты для компиляции и сборки проекта
FROM ubuntu:latest

# Установка необходимых зависимостей
RUN apt-get update && \
    apt-get install -y build-essential

# Копирование файлов проекта в контейнер
COPY . /app

# Установка рабочей директории
WORKDIR /app

# Команда по умолчанию для запуска сборки
CMD ["make"]
